The core functionality will work off of json config files
configuring serial ports, baudrate, and refreshrate can be done with a side program that opens and maybe restarts the core
if there are changes made